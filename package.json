{
  "name": "house-manager",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:server",
    "setup:app": "npm run setup:server && npm run setup:client",
    "setup:client": "cd house-manager.client && npm i",
    "setup:server": "cd house-manager && npm i",
    "start": "node house-manager/index.js"
  },
  "engines": {
    "node": "^14.17.0"
  }
}
